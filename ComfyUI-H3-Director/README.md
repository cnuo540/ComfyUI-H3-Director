# H3 漫剧导演台（ComfyUI-H3-Director）

这是一个面向 ComfyUI 原生 MiniMax H3 工作流的中文长视频导演台。它把"多段生成、逐段验收"的漫剧制作流程整合进**一个节点**：可视化时间轴、每段独立提示词与参考图、每段配音音频、点段预览、单段重抽，段间通过尾帧文件自动接力，并按当前最新分段自动合并成片。

当前插件版本为 **2.40.2**。最低支持 ComfyUI **0.30.0**，推荐 **0.31.1 或更新版本**。导演台插件 ZIP 本身不包含模型，也没有复制 H3 的模型实现；另行交付的“离线环境包 ZIP”只用于二采节点与 Python 依赖的私人迁移，**不包含3D权重**。请先确认 ComfyUI 已带有 `MiniMax H3 Reference To Video` 和 `MiniMax H3 Image To Video` 原生节点。

- `二采批量开关与视频运行解耦（v2.40.2）`：顶部“全部启用”仍只控制当前页面的视频段是否参加运行和合并；二采卡原“全部选择”改为独立的“全部二采”。关闭全部二采会立即移除各段时间线上的启用标记，但用 `second_sample_restore` 保留各段自己的分辨率、步数、降噪和其他二采参数；重新开启时优先逐段恢复，没有历史参数的段才使用当前二采默认。二采批量开关不读写 `segment.enabled`，创作、视频、文本三个页面各自只处理自己的段。

- `跨机一键配齐来源修复（v2.40.1）`：环境来源下拉只显示后端当前 `available=true` 的来源；新电脑尚未加载兼容3D节点时通常只显示“本地离线环境包（不含权重）”，旧工作流保存的不可用环境来源会安全回退到本地来源。环境安装和权重下载使用独立结构化阶段状态，权重下载失败不再显示成环境来源失败。国内 ModelScope 固定权重允许在 `modelscope.cn` 可信 HTTPS 域名内有限重定向，拒绝缺少目标、未知域名、IP、凭据、异常端口、路径越界和超限跳转；最终仍严格校验固定大小与 SHA-256，且不会静默切换到官方线路。离线环境包继续不包含权重；目标电脑真实下载、重启和 Live UI 仍需实机验证。

- `单个离线环境包（v2.40.1）`：二采环境来源选择“本地离线环境包（不含权重）”后，界面只显示一个“选择环境包 ZIP”按钮，不再提供已解压文件夹入口。与本版固定合同匹配的 ZIP 只包含 `MiniMaxH3AddNoise` / `MiniMaxH3ShiftSigmas` 节点、与当前导演台调用及缓存键兼容的 `MinimaxH3LatentUpscaler3D` 节点快照，以及 Windows x64 / CPython 3.13 的固定离线 wheels；它不包含任何3D权重。后端逐文件流式校验 manifest、路径、大小和 SHA-256，拒绝额外文件（包括 `weights/`）、路径穿越、符号链接、加密项和同名不同内容；相同节点文件跳过，缺失节点按同目录暂存后原子落位，只有缺失 Python 模块才以 `pip --no-index --no-deps` 安装固定 wheel。用户仍需在同一卡片明确选择国内 ModelScope 或官方 Hugging Face 权重线路；一键配齐先安装本地环境，再由导演台在线下载并校验固定 `minimax_h3_latent_upscaler_3d_fp16.safetensors`。不下载 H3 Base/Ref2VA 主模型、不静默换线路、不自动重启，完成后提示手动重启 ComfyUI。作者 3D 节点仓库在本次冻结时仍无可核验 LICENSE，因此该快照只允许用户自有电脑之间私人迁移；本包不是公开发布、出售、镜像或再分发许可。

- `二采采样方式选择（v2.39.9）`：创作、视频、文本三页的统一设置和逐段设置均可选择“优先分块（2块，省显存，推荐）”或“整幅一次采样（高显存）”。旧工作流和新段默认使用优先分块；它在满足现有安全条件时把同一次二采按空间分成两块并用 latent 重叠融合，以降低峰值显存，条件不满足时仍会回到整幅。整幅一次会明确禁用分块并只调用一次 H3 二采，但需要更多显存；不承诺一定能在16GB显卡运行，也不宣称画质一定更高。两种方式不改变模型、LoRA、注意力后端、时长、帧数、分辨率、步数、denoise、seed或固定沿用的一采音频。
- `二采音频固定与设置方式简化（v2.39.8）`：创作、视频、文本三页删除“冻结一采音频”选择，所有二采固定只修复视频并沿用一采对白、音色和环境声；旧工作流或旧分段中的 `freeze_audio=false` 也会在前端和后端统一迁移为 `true`，不再重新加噪或重采音频。原“多段统一设置 / 多段分别设置”下拉改为一个“多段统一设置”勾选框：勾选为统一，取消为分别；原 `uniform / separate` 工作流属性保持兼容。该修改不改变 H3 主模型、3D 放大模型、LoRA、注意力后端、DynamicVRAM、时长、帧数、尺寸、步数、denoise、seed 或画面采样数学；Live UI 和真实 GPU 仍需另行验证。

- `二采分块条件与失败回退收口（v2.39.7）`：内置空间分块会按当前块同步裁切 FL2VA 硬首尾帧的 `minimax_keyframes` latent，参考素材块仍完整复用；控制台与终态诊断记录分块方式、轴、块数、重叠、当前块或未分块原因。二采失败后不再在一采写盘前执行额外的无保护 CUDA 缓存同步，只有 MP4 实际写出后才报告“一采已保存”；若受保护的 CUDA 清理失败，会明确报告“CUDA 清理未完整完成”。本次不改变 H3 主模型、作者 3D 放大模型、LoRA、注意力后端、DynamicVRAM、尺寸、帧数、步数、denoise、seed、音频或采样数学；真实连续 GPU 运行仍需按短片基线到 15 秒顺序验证。

- `二采全部选择与紧凑开关行（v2.39.6，v2.40.2已纠正）`：v2.39.6 曾让二采卡“全部选择”复用视频段的启用状态，实际与顶部“全部启用”重复。v2.40.2 已将它改为独立“全部二采”；视频运行选择与二采启用状态不再互相改写。

- `三页面紧凑控制与二采卡重排（v2.39.5）`：创作、视频、文本三页把原“全选 / 全不选 / 尾帧全勾 / 尾帧全不勾”收为“全部启用 / 全部尾帧”两个当前页勾选框，部分状态自动显示；第1段不参与尾帧统计，单段页面禁用“全部尾帧”。每张时间线段卡新增独立“尾帧”勾选，创作页使用 Ref2VA 软参考，视频/文本页使用 FL2VA 硬首帧，原执行字段不变。文本页删除目标秒数、AI小说分析和当前段标题/种子参数条；创作页删除 AI小说分析，三页仍保留 AI剧情、AI分镜和 AI精修。重复的长拖动说明改成小拖动柄，宽高调整能力保留。二采卡只重排现有控件：标题显示启用、范围和最终尺寸徽标，环境与3D权重成为折叠行，保存、默认继承、环境检查和采样合同不变。独立视频超分页不显示分段勾选框；本次不改变模型、采样或 GPU 行为。

- `二采设置面板融合（v2.39.3）`：创作、视频、文本三页的二采标题、当前范围摘要、折叠箭头和全部设置现已融合为与“API 设置”同级、同宽的单一卡片，不再显示孤立按钮。“成片后期”前端面板已删除，合并请求也不再携带旧工作流中不可见的后期设置；后端旧接口仅保留兼容。二采保存、作用范围、环境检查、默认继承和运行合同不变。
- `时间线重抽与二采入口布局收紧（v2.39.2）`：创作、视频、文本三页顶部不再显示重复的“🎲 重抽当前段”，单段重抽统一从时间线段卡进入；卡内“重抽”缩为上一版约七成，不再接近“启用”控件大小。二采入口从顶部运行工具栏移出；其后由 v2.39.3 融合为独立折叠卡片。停用段不运行，旧视频版本不覆盖。

- `三页面紧凑重抽与新段二采默认（v2.39.1）`：创作、视频、文本三页的每张时间线卡都显示与“启用”同级的紧凑“重抽”，只给该段换新种子并生成；三页左侧“全段重抽”都只运行当前页面已启用段，完成后按最新段文件自动合并。保存一次二采设置后，三个页面以后新增的分段都会复制该默认并立即显示绿色“二采”；不同页面的数据、旧成片和已有文本二采执行链保持独立。

- `二采新段默认与创作页段级导入/重抽（v2.39.0）`：在“二采设置”保存一次后，完整规范化配置会作为当前导演台节点的默认值随工作流保存；v2.39.0 的创作页和视频页新段会复制该配置。每段仍可独立关闭或修改，不会回写默认或影响其它段；再次保存会更新未来新段使用的默认。创作页保留“分析导入全部段”，新增“导入当段”：多段脚本只取与当前卡序号对应的一段，单段脚本直接写入当前段，前后分段和全局提示词不变。创作页不再显示目标秒数、帧率、种子/随机及台词慢/标准/快建议控件，旧工作流字段和执行值继续兼容。文本页原有脚本导入方式未改变。

- `二采内置空间分块候选（v2.38.1）`：2.38.0 已在真实 15 秒、362 帧、1344×736、7 步任务中失败；一采、3D放大和二采即使拆成三个内部节点，H3 首次高分辨率 forward 仍达到 18.04 GiB 并在 `0/7` OOM。核对当前 ComfyUI 后确认，节点结束只重置动态 cast buffer、预取队列和 VBAR 水位，不会卸载 H3 ModelPatcher，因此旧三节点路径已删除。新候选回到单个受控提交节点，仅在冻结音频、denoise≤0.35、无参考图片、5D视频latent且长边至少64时，把二采视频latent沿长轴分成两块，每块完整使用同一个H3/LoRA/attention patch、同一repair conditioning、sampler、scheduler、seed和全部steps，最后以8个latent token重叠做余弦融合并拼回原一采音频。它不依赖YCNodes，也不降低帧数、最终尺寸、steps、denoise或精度；由于两块分别采样，可能更慢，接缝、画质、峰值显存及连续生成是否成功仍必须用真实GPU验证。

- `二采异常清理防崩溃（v2.37.5）`：2.37.4 真实运行在作者3D放大完成、H3二采 `0/7` 后进入异常清理，失败分支仍调用 `unload_all_models()`，DynamicVRAM 在恢复模型备份时触发 `Fatal Python error: Aborted`，导致8188服务退出并遮蔽先发生的二采异常。导演台现在不再在二采失败清理中完整卸载H3，只清理失效模型记录和allocator缓存，让控制流正常到达ComfyUI官方节点结束边界。一采回退、结构化失败信息和服务存活仍需真实GPU复核；此修改不等于已证明15秒二采成功。

- `时间线启用、安全文本载入与双模式分辨率（v2.37.0）`：创作、视频、文本三个页面的每个时间线段卡都有独立“启用/停用”复选框；停用只让运行跳过该段，不删除提示词、种子、参考素材、二采设置或历史成片。文本页“载入文本”以及创作页文件载入只把内容放入脚本框，不再自动解析或绕过确认覆盖时间线；文本页随后可明确选择“解析并替换全部段”（保留两次点击确认）、“导入到当前段”或“追加到末尾”。二采面板的百万像素、最终宽度和最终高度始终可以直接编辑：开始编辑 MP 即切到百万像素模式并按当段画幅计算对齐宽高，开始编辑宽或高即切到手动分辨率模式；持久化仍保持 MP 或宽高二选一的单一真值。

- `一采/二采内部执行边界（v2.37.4）`：2.37.3 的真实15秒任务证明，仅保留同一个 H3 并增加有限 `partially_unload` 仍会在二采 `0/7` 前遇到 VBAR 固定页和 CUDA OOM。导演台现在在“一采结束 → 3D latent放大”的内部阶段边界，先调用 ComfyUI 自己在节点结束时使用的动态 cast buffer 重置和预取队列清理，解除一采遗留的预取固定页，再按“所选3D权重实际文件大小 + ComfyUI最小推理预留”卸载其它可释放模型并对同一个 H3 做有限释放。它不直接操作私有 VBAR/dynamic_pins，不调用 `unload_all_models()`，也不替换 H3 `ModelPatcher`，LoRA、Sage/attention patch 和采样参数保持原样。2.37.3 的诊断防遮蔽仍保留；自动回归证明调用顺序和参数合同，真实连续两次5秒、连续两次15秒、峰值显存、速度和OOM改善仍需后续GPU验证。

- `三页面二采最终像素与单一尺寸真值（v2.36.0，v2.37.0 更新输入方式）`：创作、视频、文本三页面共用 `约0.4MP首采 → 学习型3D latent放大 → 独立高分辨率repair conditioning → 低降噪整段修复二采`。新二采默认按 `1.0MP` 设置最终像素，每段按自己的横竖画幅换算并把宽高对齐到32，同时显示目标MP、实际宽高和实际MP；也可切换为“按最终宽高（手动分辨率）”。MP、最终宽度和最终高度均可直接编辑，最后开始编辑的类别接管模式；切换时沿用当前显示尺寸，避免明显跳变。MP模式只持久化MP，手动分辨率模式只持久化最终宽高。旧二采段保留原最终尺寸并迁移为手动分辨率；非32宽高明确报错。对齐后的二采宽、高和总像素必须全部严格大于一采，否则阻止保存和运行；超过2MP只提示显存风险，不会静默降低。页面切换不会重复换算或触发生成。

- `中文超分结果播放链路（v2.35.1）`：成片超分右侧结果播放器和“新窗口查看”共用插件自己的只读视频路由，不再把中文结果文件名交给已知会失败的 ComfyUI `/view` 视频路径。前后端分别拒绝非 output、非MP4、盘符、绝对路径和路径穿越，后端只从 ComfyUI output 目录内返回 `FileResponse`，保留浏览器 Range 播放能力。对比区会分别等待原视频与超分视频的元数据；路由、解码、文件格式或时长异常会直接显示，不再只留下黑屏和 `00:00/00:00`。

- `一键配齐来源与显式3D权重（始于v2.36.0，v2.40.1更新）`：三个页面共用一个“开始一键配齐”入口和进行中锁。官方在线来源仍要求本机先有 `MinimaxH3LatentUpscaler3D`，再按固定提交安装 AddNoise/ShiftSigmas，并通过所选国内 ModelScope 或官方 Hugging Face 固定合同下载唯一3D权重。本地离线环境包只负责兼容3D节点、AddNoise/ShiftSigmas 与缺失 Python 依赖；3D权重始终与环境包分离，并由导演台按用户明确选择的在线线路下载。环境下拉不展示不可用来源，环境/权重终态分开显示，国内权重只进行受控 ModelScope HTTPS 跳转。两条路径均不下载 H3 Base/Ref2VA 主模型、不静默换线路，也不自动重启。

- `创作页 AI 分镜中文输出修复（v2.32.3）`：创作页 AI 分镜现在与文本页一样明确要求简体中文镜头正文、环境声和配乐说明；供解析使用的 `integrated_multimodal_description`、`overall_soundscape`、`non_diegetic_music`、`[Shot N]` 和 `At HH:MM:SS.mmm` 格式标记仍保持英文。

- `时间轴单段二采开关（v2.32.2）`：创作页段卡右上角的“二采”标记可直接单击。青绿色表示该段开启，单击后变灰并只关闭该段；灰色标记仍保留，再点会恢复该段上一次的修复模式、一采像素目标、步数、降噪、修复要求和对比保存设置；v2.39.8 起旧音频字段统一迁移为沿用一采音频。点击开关不会改变当前段选择、拖动段、触发重命名或影响其他段。

- `二采噪声组件跨电脑安装修复（v2.32.1）`：修正了 Windows Git 检出时 CRLF 换行转换导致的固定文件大小和 SHA-256 错误。固定安装清单与 `h3-latent-upscaler` 的上游提交和 GitHub Raw 原始字节一致，只用于补齐 AddNoise/ShiftSigmas 等二采噪声组件；保留固定提交、固定 host、逐文件大小和 SHA-256 校验，不覆盖已有目录。

- `创作页真实二次采样（v2.32.0）`：支持对当前页面已选择段统一或逐段设置。当前 v2.40.2 二采需要 `Comfyui_Minimax_h3_latent_Upscaler` 提供学习型 `MinimaxH3LatentUpscaler3D`、`h3-latent-upscaler` 提供 AddNoise/ShiftSigmas，以及 `latent_upscale_models` 中由配置明确指定的3D FP16模型文件；缺所选权重会明确失败，不会按目录顺序静默换成另一个。缺组件或模型会在入队前提示，不会静默降级。MP4 成片超分仍是独立后处理。

- `超分模型状态、快速刷新与内嵌对比（v2.31.1）`：模型下载、校验、安装、刷新和删除状态紧贴模型按钮显示，安装完成后自动刷新并选中新模型。模型列表改用插件轻量接口异步读取，不再请求整套 ComfyUI 节点定义；8 秒未返回会明确报超时，不再无限显示“正在刷新”。取消超分完成后的自动弹窗，把同步播放的拖动分割对比直接放入右侧“超分结果”区域；关闭页面或删除节点仍会释放两路视频资源。

- `超分模型一键安装、删除与拖动对比（v2.31.0）`：视频超分页可在用户明确点击后，从后端固定的四个魔搭模型仓库下载所选文件，完成固定大小和 SHA-256 校验后安装到当前 ComfyUI 配置的 `upscale_models` 目录；页面打开或恢复时不会联网，也不接受前端自定义下载地址，不覆盖同名文件。“删除模型”只删除当前下拉框选中的本地模型，并在执行前显示精确模型名二次确认。原视频与超分视频支持同画面拖动对比、同步播放、暂停和跳转，只播放原视频音频；v2.31.1 起对比固定显示在结果区域，不再弹窗。

- `超分模型安装与进度（v2.30.1）`：视频超分页加入模型目录与本地文件安装，并为 AI 和 Lanczos 处理显示逐帧、编码保存和完成百分比。结果区明确显示处理前后分辨率与模式：Lanczos 只扩大像素尺寸，不宣称生成新细节；AI 画质取决于所选模型。

- `独立视频超分（v2.30.0）`：导演台新增“视频超分”第四页，三个原页面的分段视频和合并成片均可送入该页。AI 模式复用 ComfyUI 官方 `UPSCALE_MODEL`，Lanczos 模式无需模型；处理保留原音频、帧率、时长、位深和原视频，不接入或改变 H3 Base、Ref2VA、FL2VA 的采样链。外部视频可直接选择或拖入；新 MP4 保存到 ComfyUI output 目录。AI 模式需要用户自行放置并选择超分模型，插件不会下载模型或新增第三方依赖。每批帧数可降低 AI 处理的显存峰值，但当前 ComfyUI VIDEO 组件仍需在内存中保存整段帧，长成片建议优先逐段超分后再合并。

- `生成视频登记与预览修复（v2.29.9）`：导演台生成或复用的分段 MP4 现在会作为标准 ComfyUI `video` 输出登记。普通多段、全部缓存命中和单段重抽完成后，任务队列可进入完成状态，并可从任务详情和“媒体资产 → 已生成”查看视频，不再只显示最后一帧图片。媒体登记只使用 ComfyUI output 目录内的相对路径，不暴露电脑绝对路径；模型、采样、提示词、段文件、尾帧、音频和自动合并逻辑不变。

- `三页面合并与成片查看修复（v2.29.7）`：创作、视频、文本三个页面共用的“查看成片”入口始终可见；无成片时禁用，已有成片或合并成功后可在导演台内直接播放，也可另开窗口。手动合并会显示检查、合并和失败状态，成功后立即打开播放器；创作时间线中的外部视频可在仍有未生成空白段时单独合并，空白段会被跳过，真实文件缺失仍由后端明确报错。

- `三页面时长自由控制（v2.29.8）`：删除创作页最低 6 秒、视频/文本页最低 8 秒的人为限制。三个页面都可通过时间线点选、拖动和页面已有的数字/加减控件，在 H3 单次真实可生成的 `2–15` 整数秒范围内自由调整；超过 15 秒的内容继续用多个生成段完成，界面不会显示模型实际无法生成的假单段时长。

- `导演台整数段时长（v2.29.6）`：创作、视频、文本三个页面的生成段时长统一保存为 2–15 的整数秒；时间线点选、拖动、视频页输入和加减按钮都使用整数，旧工作流与成片项目中的小数段时长在恢复和保存时自动归一化。提示词内部 Shot 时间码、音频裁剪点与外部视频真实时长保持原精度；后端把整数请求换算为最接近的 H3 合法帧数，缓存预期和实际采样共用同一规则。

- `创作页直接 Picture 参考修复（v2.29.5）`：当前段“分析导入资产”的可见提示词继续保持原文，隐藏执行副本仅按当前实际参考顺序修正资产 ID 和 `<Picture N>`；普通提示词不再被强制包装为 Ref2VA 六字段或改写为 `<Subject N>`，正式 Ref2VA 六字段仍完整保留。执行前会清理 `<Picture 1>>`、`<Subject 1>>` 等重复闭合符；运行报告新增请求图片、成功加载图片和 `minimax_refs` 图像条件块数量，数量不一致只警告、不阻断生成。

- `媒体写出与缓存准备优化（v2.29.4）`：强制重抽当前段不再先打开旧 MP4、解码首尾和尾部窗口验证一个必定不会复用的缓存；旧视频和版本关系仍保留。静音段首次编码已直接生成无音轨 faststart MP4，不再启动第二个 FFmpeg 做无损复制；模型音频转交 FFmpeg 前删除一份重复 float32 内存复制。文本页旧工作流的共享参考图改为条件节点或实际生成时按需加载，单段官方缓存和全缓存命中不再提前读图；视频 VAE 解码后的引用按 Python 规则立即释放，GC/显存缓存统一留到段落安全写盘后执行一次。采样前为 VAE 腾显存的清理仍保留，模型、画质、音频、帧数和全部生成参数不变。
- `解码与帧转存优化（v2.29.3）`：视频 VAE 输出继续按 float32 执行原有 clamp/round，但在当前设备上转成 RGB8 后再传到 CPU，减少约四分之三的帧传输量并删除 NumPy 大临时副本；本段静音或自定义音频 `replace` 时跳过最终不会使用的 H3 音频 VAE，模型音频、`mix` 和环境音路径保持原行为。运行报告分开显示视频 VAE、RGB8 转存、音频 VAE、MP4 编码及 `match/max` 参考图模式；不自动修改分辨率、帧数、参考图模式、Steps、Sampler、Scheduler、Seed 或采样数学。
- `官方条件缓存与 Ref2VA 提示词（v2.29.2）`：单段首次生成和重抽当前段都按官方工作流把参考素材/Qwen3-VL 条件编码与 Seed 采样拆成两个 ComfyUI 缓存节点；提示词、参考素材、尺寸和时长未变时，只换 Seed 不再重复条件编码，首次生成或修改条件时仍会正常重算。创作页有图片或尾帧参考时统一保存官方 Ref2VA 六字段，正文使用 `<Subject N>`，运行前再次按实际 Picture 数量删除旧 Subject/Picture，并把音频、视频保留声明合并到同一套字段中，不再追加重复字段。
- `参考图绑定与耗时诊断（v2.29.1）`：创作页会在保存和运行前按当前段真实参考图顺序重建 `Subject/Picture` 绑定，删除已移除资产留下的越界 Picture，并在尾帧开关变化时同步编号；后端对旧工作流再做一次实际图片数量校验。AI剧情和 AI分镜不得擅自改写已绑定角色的脸、发型、年龄、体型、帽子、固定服装或标志性配饰。普通段不再重复执行模型清理，运行报告新增实际分辨率、对齐后帧数、图片参考数量及条件编码、采样、解码、编码、完整性验证和清理耗时。
- `创作页“成片”项目库`：可在任意制作进度保存、新建、复制、重命名、删除和切换成片。每个成片使用独立 `project_id`，切回后恢复分段、脚本、资产、人物音色、尾帧设置、历史视频关系和合并顺序；“清空分段”或“全部清空”后可恢复最近一次清空前状态。切换不要求完成固定段数，正在生成或合并时才会短暂禁用。
- `创作页本地视频命名与面板内画布导航`：创作页每个已生成视频版本可在标题栏修改本地 MP4 名称，并通过文件夹按钮在 Windows 文件资源管理器中定位；插件保留隐藏的段号和版本号前缀，历史版本、尾帧、删除回退和最终合并关系不变。鼠标位于导演台内部普通区域或视频画面时可直接滚轮缩放 ComfyUI 画布；脚本、资产和视频列表仍有内容可滚动时优先滚动，到达边界后再交给画布缩放，`Ctrl+滚轮`始终缩放。按住鼠标中键拖动可平移画布，并按当前画布缩放比例换算拖动距离。

## 主要功能

- `创作页电影化 AI 模式与资产映射（v2.29.0）`：创作页点击 `AI剧情` 可从自动电影化、忠实改编、悬疑、恐怖、动作、科幻、灾难、情感、喜剧、冒险、对白冲突和强钩子短剧中选择；点击 `AI分镜` 可选择调查、门后悬疑、身份验证、动作启动、追逐、情绪决定、恐怖揭示、对话冲突、载具故障、喜剧误会、浪漫靠近和科幻异常等电影化镜头配方。提示词融合 H3 电影化分镜技能及公开官方视频提示方法，但运行时不新增联网请求。当前段提示词下方显示只读彩色 `@资产 → <Picture N>` 映射；勾选上一段尾帧时会明确占用 Picture 1，后续编号自动顺延。文本页、视频页、API、模型和生成参数不修改。
- `AI资产锚点与段间外部视频（v2.28.3）`：创作页和文本页的 AI剧情、AI分镜会要求并在本地恢复唯一匹配的彩色 `@编号（完整名称）`，避免文本模型只留下普通名称；官方 Base/Ref2VA 字段、声音字段及解析后的 Picture 映射保持原规则，不增加审核或第二次 API 请求。创作页时间线可把本地 MP4/WebM/MOV/MKV/AVI 拖到段与段之间，复制件保存在当前导演台项目并支持预览、移动和删除；插入视频只按时间线顺序参与最终合成，不占生成段编号，也不作为下一段尾帧。
- `当前段直接输入与 @资产（v2.28.2）`：创作界面保留整篇“解析导入”，同时允许直接编辑当前段提示词并即时保存。当前段输入 `@` 或点击工具栏小 `@` 可选择资产；插入结果保留彩色 `@编号（名称）`，并同时写入 H3 使用的 `<Picture N>` 或 `<Audio N>`。人物绑定音色时继续自动加入对应音色。只影响当前创作分段，不修改上方整篇脚本、其它分段、文本页、视频页、AI、API、模型或生成参数。
- `界面宽度兼容修复（v2.28.1）`：导演台 DOM Widget 在首次挂载、工作流恢复和节点缩放时显式跟随灰色节点外框宽度，避免部分 ComfyUI 前端把黑色界面固定在左侧窄列。创作界面资产库和当前段实际参考继续支持横向、纵向拖动，并限制恢复宽度不超过当前栏位。未修改提示词、AI、API、模型、采样、视频生成或工作流数据。
- `资产库清空、图片放大与视频版本删除（v2.28.0）`：创作界面资产库提供二次确认的一键清空，同时清理图片、参考音频、资产槽和分段引用，但不删除磁盘原文件；点击资产图片可在页面内放大查看，拖入当前段的操作保持不变。创作、视频、文本三个页面共用的视频区域为每个本地版本增加二次确认删除；删除历史版本不影响当前版本，删除当前版本会回退到剩余最高版本并同步对应尾帧，删除最后一个版本后该段恢复为待生成状态。提示词、资产、其它视频版本和已生成的旧合并成片不受影响。
- `创作界面 @资产与人物音色（v2.27.0）`：顶部“漫剧”页签恢复显示为“创作界面”，内部 `create` 数据和旧工作流字段不迁移。资产编号按稳定 ID 使用不同颜色；资产库新增稳定 `A1/A2...` 参考音频，可选择音色参考或配音驱动。角色卡可绑定人物音色，整篇剧本或当前段明确输入 `@人物`时会同时加入人物图片与绑定音色；明确 `@A#`只进入对应分段，单段继续限制最多 3 路 H3 原生参考音频。原独立参考音色上传区迁入资产库，并增加不删除磁盘文件的“清空资产库”操作。视频页、文本页、模型、采样、尾帧、合并和 API 配置未修改。
- `纵向视频与全页伸缩（v2.26.0）`：灰色 ComfyUI 节点外框与黑色导演台页面统一使用当前 DOM Widget 实际分配尺寸，页面不再从节点底部溢出。漫剧页的视频预览移动到左列当前段提示词下方；漫剧、视频、文本三页的 1～4 个视频窗口统一纵向排列，视频区域可上下拉伸并按页面保存高度。漫剧脚本中的 `@资产编号（名称）` 使用蓝色高亮，输入、选择、解析和中文输入法行为不变。未修改工作流数据、生成参数、模型、采样、API 配置或已有媒体。
- `FL2VA 自由运行（v2.25.1）`：删除主 `model` 为 FL2VA 时参考图、参考音频、参考视频和硬尾帧组合的运行前阻断。单 FL2VA 工作流会继续生成，Ref2VA 专属参考素材不会送入模型；插件根据当前连接到 `model` 的上游模型自动选择正确路径。
- `漫剧资产与多版本对比（v2.25.0）`：原“创作界面”显示名改为“漫剧”，内部 `create` 数据不迁移。漫剧、视频、文本三个页面的视频预览均可用 `＋/－` 显示 1～4 个段级历史版本；重抽仍按编号保存，不覆盖旧视频。漫剧页支持结构化 JSON、中文档案资产提取和未绑定资产槽；漫剧分镜脚本、当前段提示词及视频页提示词输入 `@` 可选择资产，中文输入法确认不会误提交。
- `资产库命名与双区伸缩（v2.24.34）`：创作页“全局资产库”统一改名为“资产库”，“全局参考”勾选语义保持不变。资产库和当前段实际参考分别增加与分镜脚本一致的底部拖动条，可上下调高、左右调宽；两个区域独立记住尺寸，切换分段、增删资产和保存工作流后继续保留。文本页、视频页、资产分配、模型和采样不修改。
- `创作页约 6 秒时长档（v2.24.33）`：创作页时间轴手动拖动可到最接近 6 秒的 H3 合法档位 141 帧（5.875 秒，界面显示 5.9 秒）；文本页和视频页仍沿用原手动范围。模型、采样器、Steps、Scheduler、Seed、音频和旧缓存均不改变。
- `资产库与可选全局参考（v2.24.32）`：创作页把旧“全局参考资产库”改为独立资产库。上传的角色、场景、道具和通用图片默认只保存在资产库，不再按提示词名称或别名自动分配；可直接拖入当前分段。每张资产新增“全局参考”勾选，勾选后才会进入所有创作分段，不勾选时只进入手动拖入过的分段。全局参考固定排在各段 Picture 编号前面；插件不会按资产分类或总数裁掉已选择的参考图。文本页和视频页不修改。
- `分段与合并保留历史版本（v2.24.31）`：同一段重抽会依次保存为 `_00001_`、`_00002_` 等新文件，预览、尾帧、缓存和合并只读取完成元数据记录的当前版本；最终合并成片同样按编号新建，不再覆盖正在播放的旧文件。
- `远程 API 小说分析与尾帧计划（v2.24.30 历史功能）`：该版曾在文本页和创作页提供目标总时长与 `AI小说分析`，把普通小说整理为生成段和尾帧计划。v2.39.5 已按界面收紧要求删除这两页的可见目标秒数与 `AI小说分析` 入口；现有脚本解析、`director_import_manifest` 尾帧策略和旧工作流数据仍兼容。
- `显式生成段边界守恒（v2.24.29）`：文本页和创作页在原剧本明确写出“每段N秒”或“共N段”时，把连续场次边界写入 `director_import_manifest`，官方 Base 导入优先按这些边界切段；例如24秒、每段8秒会稳定导入为3段8秒。没有明确分段要求时继续使用原有约15秒自动装桶。边界不是现有 Shot 起点时阻止错误导入，不再静默改成两段或产生12.3秒漂移。
- `内联分镜与直接运行修复（v2.24.28）`：`分镜1: "0s-2s: ..."`、`第1分镜`、`镜头1` 等连续绝对时间稿按完整分镜边界装入不超过 H3 原生上限的生成段，不再按字数把 104 秒误估为 538 秒。脚本框有未导入修改时，“运行/重抽当前段”按当前时间轴、当前段提示词和已挂参考素材直接执行，只显示非阻塞提示，不再自动解析并覆盖时间轴。尾帧续接方式由当前连接到 `model` 的上游模型自动决定。
- `页面隔离与手动 API 配置（v2.24.27）`：工作流恢复时页签高亮、当前数据集和正文编辑器原子同步，避免“创作界面高亮但显示文本界面内容”。删除 API 自动识别按钮和 Key/Base 猜平台逻辑；只有用户主动选择预设时才填入预设地址和模型，输入 Key 不再改写服务商、API Base 或模型。
- `长任务自动恢复与安全释放（v2.24.26）`：每段安全写盘后自动执行轻量清理；实际运行电脑达到内存压力安全线、当前段异常/取消，或用户在视频页点击“段后深度释放”时，才调用 ComfyUI 深度卸载，下一段重新加载可能稍慢但不会中断当前采样。MP4、无损尾帧和完成 JSON 组成原子段级检查点；旧缓存验证有效后直接升级，损坏或半写文件不会误命中。前三段完成、第四段失败时，下次从第四段继续。新增段号、耗时、内存状态、清理动作和续跑起点日志；未修改模型、采样、分辨率、Steps、Sampler、Scheduler、Seed、API 或创作/文本页面。
- `AI 与节点界面恢复（v2.24.25）`：创作页、文本页恢复 `AI剧情 / AI分镜 / AI精修`，当前段可独立精修；视频页只恢复当前视频提示词 `AI精修`，三个页面的数据仍完全独立。API Key 支持本机安全识别：OpenRouter 与 OpenAI 专用前缀可自动填写接口，普通 `sk-...` 不猜服务商、不向多个站点试探；也可粘贴“接口 + Key + 模型”整段配置。增加旧工作流/加载顺序异常的专用 UI 补挂，内部 JSON 控件不再裸露。未修改模型、采样器、分辨率、Steps、Sampler 或 Scheduler。
- `全局参考资产与运行修复（v2.24.24）`：创作页把资产库与当前段实际参考分开；图片文件名、资产名和别名可自动匹配到每段，手工引用可保留并锁定。删除横排分类助手和导入到当前段入口。运行前自动同步已修改脚本；参考素材与尾帧续接按当前连接到 `model` 的上游模型能力处理。窄节点改为安全最小宽度和单栏响应式布局，不修改模型、采样器或生成参数。

> 当前界面以 v2.40.2 为准。文本页与创作界面保留独立的 `AI剧情 / AI分镜 / AI精修`，不再显示目标秒数或 `AI小说分析`。创作页和文本页的 AI剧情、AI分镜均直接使用自动电影化规则，不显示模式选择；AI 结果只写回当前页面输入框。视频页仍只处理自己的视频提示词，不会被文本/创作界面内容覆盖。

- `横排隐蔽式创作工具栏（v2.24.23）`：主输入框恢复完整宽度；“补充：剧情、人物、场景、镜头、动作、声音、结尾”和“AI精修选中”恢复为输入框下方横排工具栏。按钮原有动态语义建议、光标插入、局部AI精修和中文输入法保护保持不变；空文本不显示建议，不弹对话框、不自动确认。文本与创作界面共用同一实现，视频界面及所有生成参数不变。
- `场次时间轴/API/局部精修/显存修复（v2.24.22）`：AI故事的“场次1＋场次起止时间”按权威绝对时间解析；15秒内多个场次保持为一个Director生成段，长故事只在完整场次边界装桶。API已收到响应但没有最终文本时不再误报连接失败，并按当前服务商给建议；文字连接测试只把参考图能力标为模型名推断。局部精修保留“画面风格、视觉风格”等非台词字段。采样完成后在VAE解码前释放不再需要的采样对象，减少峰值重叠，但不恢复8GB模式、不改模型、步数、分辨率、Sampler或Scheduler。
- `框选局部AI精修（v2.24.20）`：分类栏右侧提供“AI精修选中”。未框选时按钮禁用；框选后只发送并替换该段文字，其余草稿保持不变。API错误只显示在状态栏，不触发内容审核或自动二次修正。
- `内嵌创作引导（v2.24.19）`：撤销 v2.24.18 的左侧页面导航，恢复输入框下方的内嵌候选。输入一句主题时出现“继续搭建你的草稿”；输入“剧情：”“人物：”“场景：”“镜头：”“动作：”“声音：”或“结尾：”时出现对应候选。点击只插入光标处，不覆盖其它文字、不生成整篇提示词、不自动确认。文本界面和创作界面一致，视频界面不变。
- `输入区说明精简（v2.24.15，v2.24.19恢复内嵌交互）`：文本页和创作页统一为“创意与分镜草稿”。删除输入框中失效的固定步骤长文，也不把官方字段名和自动分段规则堆在输入区上方；候选只在输入命中主题或字段时临时出现。官方 Base / Ref2VA 的直接粘贴解析能力保持不变。
- `自由草稿与光标辅助（v2.24.14，v2.24.19）`：主输入框不会默认进入固定模板或11步流程；中文输入法组合期间不会触发引导。输入完成后，内嵌候选只提供当前需要补充的内容；点击具体项仅插入光标处。AI故事整理保持单次调用，非空结果直接写回；关闭剧情具体度、装备名称等内容审核和自动二次修正。移除可见生产前检查、自动修复、检查报告、导入统计及运行前拦截；真实API、后端或模型错误仍正常显示。官方Base转换仍保留三字段、绝对Shot时间和时长解析等必要技术门禁。
- `删除8GB优化模式`：移除顶栏“8GB优化”和“FFN分块(实验)”按钮，后端不再自动把采样压到20步、不再把参考视频降到8fps/最长边768，也不再给模型追加导演台内置FFN补丁。历史工作流中的隐藏旧开关和段级标记会被安全清理，不改变用户当前步数、参考视频帧率或上游模型链。逐帧写视频、磁盘分段缓存、轻量预览返回和自动合并属于通用稳定性能力，继续保留。
- `三份参考提示词优点内化（v2.24.10）`：30套模板都吸收了三类可复用优点：官方Base三字段与绝对Shot时间、主体身份和限定风格锁定；同一空间连续动作、摄影机慢半拍或由主体动作触发、同步现场声、与当前类型相关的正反向限制和结尾回收；高速动作的接触帧、重力/惯性/绳索张力/受力方向和落点连续。只借鉴镜头语法与组织方式，不复制参考提示词中的人物、地点、品牌或商业角色。
- `本地主题识别与具体候选（始于v2.24.8，v2.24.19恢复内嵌显示）`：导演台在本地识别草稿主题并生成具体人物、造型、场景、布局、道具、产品动作或手绘变形候选；候选在主输入框下方临时显示，点击只插入当前光标，不会自动确认或一次生成完整提示词；不调用API。
- `具体剧情进入镜头与15秒分组对白（v2.24.8）`：具体方案中的人物行动、证据、接触和结果会真正分配到Shot，不再把人物名单或“关系冲突：误会→证据”半句当作画面动作。所有支持语言的模板按每15秒Director生成段显示多句对白编辑器；待填台词行会保留供继续编辑，但占位台词、跨段、重叠、超时或过快台词仍会在生成前阻止。最终始终输出官方Base三字段、首镜无时间戳、后续Shot绝对时间；30秒仍对应两个约15秒Director生成段。
- `H3镜头可执行性与剧情因果门（v2.24.5）`：整篇 AI 现在明确建议每15秒约3–5镜、最多6镜，单镜通常2–6秒；本地不会再因单个短确认镜头阻止解析、复检或运行，但仍会拦截平均镜头过短和30秒被扩成几十个Shot，并自动要求 API 合并重写一次。同步检查“复杂剧情/复杂运镜”等抽象占位、装备未命名或选择后未使用，以及原生2D像素风混入环绕旋转、立体透视、PBR/体积光等三维语言。生产前检查还会提示无像素参考图的风格漂移、尾帧传播风险，以及游戏/像素内容未声明画幅时当前工作流实际采用的横屏或竖屏。
- `API 返回兼容与智谱预设（v2.24.4）`：新增智谱 AI 正式预设，可识别 `https://open.bigmodel.cn/api/paas/v4`、尾斜线及完整 `/chat/completions` 地址，并提供 GLM 文本/视觉模型下拉。后端兼容 OpenAI/Responses、智谱、Gemini、Anthropic 和常见中转站的最终文本结构；若推理模型只返回思考过程，会明确建议切换普通 Chat/Instruct 模型，不再笼统显示“没有可用文本”。
- `续接脏帧自动回退（v2.24.3）`：生成段写出尾帧、旧项目再次运行以及“从视频续接”上传都会检查最后 24 帧。若最终帧出现突发黑/白帧、单色坏帧、彩噪、棋盘格、边缘密度异常或相对前序画面的极端跳变，会自动依次回退 N-1、N-2……；仅在尾帧内容实际变化时才更新 PNG，使受影响的下一段缓存失效，正常项目不会整批重跑。全部候选均异常时明确禁用该尾帧，避免把花屏确定性写入下一段开头。
- `逐帧流式视频写出（v2.24.3）`：写入 FFmpeg 时按帧发送 RGB24 数据，不再先调用 `frames_u8.tobytes()` 创建整段连续字节副本，可显著降低长段/高分辨率视频编码时的额外系统内存峰值和换页风险。该优化不改变画面、音频或编码参数，也不降低 H3 模型推理本身的显存占用。
- `上游模型链自由接线`：导演台不再扫描、阻止或警告 Turbo LoRA、TurboSampler、TE-Speed、TeaCache、SageAttention、LowVRAMAttention、ChunkFeedForward、Compile 等外部模型节点；用户接入的 `MODEL` 对象按当前工作流直接运行，插件不会自动断线、改参数或替换第三方实现。
- `检查→安全修复→复检闭环（v2.24.6）`：文本页和创作页的生产前检查与修复器共用同一套占位符识别。按钮会同步修复脚本正文、节点脚本属性、当前待运行分段和可安全处理的全局冲突，并立即复检；只自动填写画风、景别、运镜、光线、通用动作因果、环境音等可由现有模板确定的内容，不编造角色名、品牌、文案、台词、歌词、素材或 Picture 编号，也不改镜头时间与分段时长。
- `30套模板事件型镜头编译（v2.24.7）`：全部可视化模板保留各自官方技能或创作职责，但最终Base不再反复输出“本镜唯一职责/执行已选职责/本镜新增信息”等内部元语言。每个Shot改为自然的空间状态、可见动作、动作后摄影机响应、落定结果和画面内转场触发物；产品、品牌、MV、纸艺、拼贴、手绘实拍、游戏UI、装备关卡、像素、水墨、动作和几何预告使用互相隔离的执行语法。双人游戏由角色主动确认Continue，只保留Continue为唯一准确文字，并用纸片翻页替代无来源白闪。生产前检查新增空动作、无结果、无触发运镜、多文字和无来源闪白等非破坏性提示。
- `脚本/分段同步检查（v2.24.2）`：修正 AI 完成后的错误操作顺序，明确要求先“⚡ 解析导入”、有旧段时二次确认替换，再执行生产前检查。生产前检查把“导入时长”改称“当前执行时长”，并在脚本源时间轴与旧分段不一致时同时显示脚本解析段数、当前段数和正确处理步骤；硬性分段/时长错误不会被“无需修复”的文案掩盖。
- `AI 时间/风格硬契约（v2.24.0）`：`AI 故事→剧本` 会锁定用户明确给出的总时长及“前15秒3D、后15秒2D像素”等绝对区间，并校验场次必须从0秒连续覆盖到目标结束；`AI 剧本→H3官方分镜` 会验证边界时间必须正好对应一个 Shot 起点，目标风格从该 Shot 第一帧开始。若 AI 把15秒边界拖到18/20秒，插件不会覆盖原稿，而会自动调用 API 修正一次并再次本地复检。Director manifest 会保存机器可读的 `hard_style_phase_N`，生产前检查可继续阻止语义漂移。
- `跨风格尾帧安全（v2.24.0）`：生产前检查只比较上一段最后 Shot 与下一段第一 Shot 的排他渲染域；3D/实拍→2D/像素/动画且仍勾选尾帧时显示橙色警告，但不会阻止生成。FL2VA 硬首帧可能让开头保留上一段风格；Ref2VA 跨风格软续接只保持身份、构图和运动，不强制保留旧渲染风格。若上一段末镜已经完成像素化，则正常允许尾帧接力。
- `声音与合并质量修复（v2.24.1）`：删除音频中段时会先把保留的首尾两段时间戳归零，避免后半段异常静音或被视频时长截掉；完整视频合并时使用 32 kHz 双声道无损 PCM 中间音轨，只在最终成片编码一次 AAC，防止每段 AAC 填充累计造成音画漂移和重复有损压缩；“模型原声 + 自定义音频”按界面设定直接混合，不再被 FFmpeg 默认额外减半，并继续使用 0.90 安全限幅防削波。
- `短尾段重平衡、官方模板门与清晰复检（v2.23.7）`：长官方时间轴仅在既有完整 Shot 边界重分配生成段；只有能减少少于 5 秒的短尾段、且不增加 H3 生成任务数时才启用。无法在完整 Shot 边界解决的短段会保留真实提示，不会改写故事时长、镜头或台词。AI“剧本→H3 官方分镜”在写回前会本地验证 Base 三字段、严格 Shot 时间轴和源时长清单；仅缺首字段且其余结构完整时才自动补齐，任何不完整 Base/Ref2VA 都会被四个导入入口和生产前检查阻止。现名“仅修复全局并复检”的按钮会明确区分“全局无需修复”与“仍有非阻塞时长建议”。
- `界面提示词隔离（v2.23.8）`：创作界面和文本界面的全局提示词分别保存；视频界面不再继承任何隐藏的创作/文本全局提示词。旧工作流首次加载时会把原共享全局内容归到当前创作或文本工作区，视频工作区始终从空全局开始；后端同时提供视频模式防线，避免旧前端或旧工作流再次把创作剧情拼入参考视频提示词。
- `跨段画面与剧情连续性（v2.23.6）`：长官方时间轴拆成多个生成段时，会把 `overall_soundscape` 局部化为当前段 Shot 的声音，防止后段场景、怪物和动作提前泄漏；运行时追加严格的分段范围契约。检测到 FL2VA 时，下一段使用上一段真实尾帧作为强制首帧，并对前 8 帧做不改帧数、不改时长、不改音频的确定性桥接；Ref2VA 尾帧关系写入既有六字段正文，不会重复官方字段或把 Base 混成 Ref2VA。
- `全局提示词一键修复（v2.23.4）`：文本与创作界面在“生产前检查”旁提供安全修复按钮。当 AI 把“前半段 3D、后半段 2D 像素”、分段场景、构图或运镜误写进全局框时，按钮会从全部分段重新提取角色身份、外貌、服装、固定道具及无字幕/无水印限制，清除冲突内容并立即复检；缺少 Picture、素材、用户真实名称或官方字段等不能安全推断的问题仍会保留。
- `API 网络回退（v2.23.3）`：优先遵循系统代理；只有在代理或连接建立阶段失败时，才自动改用直连重试一次。鉴权、余额、限流、模型不存在及已收到 HTTP 响应的错误不会触发直连重试，避免重复消耗 API 请求。
- `一体节点`：提示词编辑、参考素材、音频管理、采样、解码、保存和预览集中在单个导演台节点；模型与 VAE 仍由外部标准加载节点连接，便于复用现有工作流。
- `左右工作台（v2.20）`：创作、视频、文本三个界面均改为宽屏左右工作台；左侧集中脚本、段设置和提示词，右侧集中参考素材或视频预览；窄窗口自动回到单栏，不改变旧工作流数据。
- `跨段连续性修复（v2.23.2）`：全局提示词不再自动吸收首段场景/构图；3D 与 2D/像素等硬冲突会在运行前阻止，后端对旧工作流再做一次冲突过滤。尾帧续接段自动加入人物、位置、动作、镜头和光线连续性约束，禁止生成开头跳回上一场景。
- `官方时间轴规范化（v2.23.2）`：自动清理 AI 常生成的 `[Shot 1] 起止范围`、`At 时间 + 重复起止范围`，导入后统一输出官方 `[Shot 1]` / `[Shot N] At ...` 结构。
- `队列兼容修复（v2.23.1）`：导演台运行时向 ComfyUI 队列同时传递裁剪后的执行图与完整工作流元数据，兼容 Easy-Use 的全局种子包装、rgthree 队列包装和原生队列，避免点击“运行”时报 `seed_widgets` 未定义。
- `可视化时间轴`：每段一个格子，三个页面都可拖右缘或点时长标签在 H3 原生 `2–15` 整数秒范围内自由调整；`+段` 可继续增加总时长，也可单独停用某段。
- `每段独立配置`：提示词、种子、时长、输出帧率（8~24fps）逐段可调。
- `全局提示词`：创作与文本界面分别保存自己的全局提示词，视频界面固定使用空的独立全局提示词。全局内容会拼到当前工作区每一段提示词前，只适合写角色身份、固定服装/道具和无字幕/无水印等真正共享限制；分段风格、场景、构图和运镜必须留在对应 Shot。
- `角色/场景/道具资产库（v2.23）`：四类参考图分组显示，名称默认取文件名；同一文件改类型时只移动分类、不复制图片。项目内使用稳定的 `C1/C2`、`S1/S2`、`P1/P2`、`G1/G2` ID，生成具体段时再按该段图片顺序映射为 `<Subject N>` 与 `<Picture N>`，不会因为别段少一张图就串号。
- `参考图按段上传`：缩略图自动编号，写 `@图1`（或 `@image1`、`【图1】` 等）自动转换为 H3 需要的 `<Picture N>` 标签；点缩略图直接在光标处插入正确编号，不会写错。
- `本地资产匹配 + API 兜底`：官方 Ref2VA 优先在本地读取 `<Subject N>/<Picture N>`；普通剧本先按角色、场景、道具完整名称本地匹配，只有未匹配段才调用 API。API 不可用时仍会安全回退，并要求逐段检查。
- `故事→剧本→H3官方分镜（v2.23）`：创作和文本工作台共用顶部一套 API 配置。整篇 AI 会先读取参考资产的类型与完整名称（不会发送本地路径），保持目标总时长，随后输出 H3 官方 Base 三字段与贯穿全片的绝对 `[Shot N] At HH:MM:SS.mmm` 时间轴，最后附加只供导演台读取的 `director_import_manifest`。
- `当前段 AI`：`✨ 优化当前段` 只改当前选中段，不改整篇剧本和其它段；与整篇 AI 共用顶部 API 设置，不再重复显示第二套接口。
- `自动完整合并（v2.23）`：界面不再提供高内存“汇总输出”选项。节点只返回轻量尾帧预览，各段完整音画保存在 MP4；全部生成或重抽当前段完成后，自动按最新文件合并。例如重抽第 3 段后自动得到 `1-2-新3-4`。
- `中文档案分镜直导（v2.21.1）`：优先识别 `[整体风格]/[角色档案]/[道具档案]/[场景档案]/[分镜N]`；一个 `[分镜N]` 始终对应一个生成段，段内的 `0.000—1.800秒` 等动作时间块不会再被错误拆成多段；显式 `时长：5秒` 优先用于自动时长，编号资产名和 `[场景名]-标题` 也会自动清理。
- `生产前检查（v2.21）`：解析或生成前检查单段 5–15 秒、核心运镜≤6秒、台词速度、完整名称、`同上/如前/位置不变`、代词歧义、未替换占位符、缺失素材、Picture 编号/尾帧偏移、声音字段与多段配乐跳变风险；解析导入后状态栏会自动显示错误/警告数量。
- `模板识别标签（v2.21.1）`：文本页与创作页统一显示 `官方 Base`、`官方 Ref2VA`、`中文档案分镜` 等识别结果；没有上传参考图时仅提示可按纯文本生成，不再把档案资产误报为素材文件错误。
- `时长守恒门槛（v2.22）`：官方 Base/Ref2VA 从绝对 Shot 时间轴或导演台清单取得源时长，中文档案分镜汇总显式时长；导入时只允许 H3 合法帧档位造成的小幅吸附偏差。文本页与创作页的整批导入/当前段导入都会拒绝明显膨胀，生产前检查显示“脚本源时长 → 当前执行时长”，普通文本则明确标为估算。
- `官方字段交付门槛（v2.22）`：每个生成段都必须保留 Base 三字段或 Ref2VA 六字段及正确顺序；缺字段、字段顺序错误、Shot 时间不递增，或把 `director_import_manifest` 混入 H3 段提示词，都会阻止导入或生成。
- `官方 Base AI 格式`：段级 AI 现在输出 `integrated_multimodal_description / overall_soundscape / non_diegetic_music` 三字段；首镜无时间戳，后续镜头使用绝对时间，台词使用 `(S1)` 与 `<d>[Chinese]…</d>`，默认 `non_diegetic_music: N/A`，降低多段配乐和声音声明互相冲突的概率。
- `尾帧接力`：每段生成后自动把最后一帧存盘，下一段勾选"续接上段尾帧"即从此画面无缝继续；取消勾选即可换场景换人物。也可用"从视频续接"上传任意 mp4 抽尾帧当起点。
- `段级音频`：每段可选 `模型音频（H3 生成）` / `参考音频驱动（对口型）`；参考音色槽 A1~A3 给不同角色配指定嗓音；支持波形可视化裁剪、起始偏移、整段静音开关；AUDIO 轨道一眼看清每段用什么声音。
- `点段预览`：点选段落即可在节点内播放该段已生成的视频（带声音、可自由缩放），想重跑哪段一眼定位。
- `单段重抽 + 自动重合并`：只给当前段换种子并重新生成，其他段原样保留；例如重抽段 4 后会自动得到 `1-2-3-新4-5` 的最新合并成片。
- `启用段合并保护（v2.20.1）`：手动合并和全部生成后的自动合并只包含当前启用段；单段重抽时使用运行前的启用快照，并强制包含当前重抽段，避免临时停用其它段造成只合并一段或把已禁用的旧片误加回来。
- `音频稳定化`：模型原声、替换、混合、环境音和最终合并统一为 32000Hz 双声道，修正时间戳与直流偏置，保守限幅并对分段首尾做极短淡化，降低砂声、削波和连接爆音。
- `无对白防伪语言`：提示词未包含精确 `<d>...</d>` 台词时，后端会追加最高优先级禁语音契约，禁止对白、旁白、歌词、耳语、咕哝和伪语言；参考视频默认只发送画面，不发送原音轨。模型音频写出前还会清理 NaN/Inf 和异常超幅，避免无效波形变成爆音或方波砂声。
- `单段合并免转码`：项目只有一个启用段时，合并成片直接原子复制已验收段文件，不再重新编码画面和 AAC；多段项目仍使用 32kHz 双声道无损 PCM 中间音轨并只在最终成片编码一次 AAC。
- `全面板缩放`：面板顶部拖条放大整个节点；提示词、参考图、预览区各有独立拖条。
- `分段与合并同时保留`：每段仍保存独立 MP4，可直接拖进剪辑软件；全部生成或单段重抽结束后会自动按当前分段重新合并，顶栏也可随时手动合并。
- `项目隔离`：每个导演台节点保存独立 `project_id`，不同工作流/不同导演台的段视频、尾帧和缓存不会互相覆盖。
- `省内存汇总`：始终只向 ComfyUI 返回最后一段尾帧和静音占位，完整音画仍保存在每段 MP4 并自动合并；不再提供高内存完整张量汇总。

## 安装

将整个文件夹放入：

```
ComfyUI/custom_nodes/ComfyUI-H3-Director
```

安装 Python 依赖（用 ComfyUI 自带的 python 执行）：

```bash
# 便携版（python_embeded）
python_embeded/python.exe -m pip install -r ComfyUI/custom_nodes/ComfyUI-H3-Director/requirements.txt

# 国内镜像加速
# -i https://pypi.tuna.tsinghua.edu.cn/simple
```

核心依赖只有 `imageio-ffmpeg>=0.6.0`（自带兼容音频稳定化参数的 FFmpeg 二进制，写视频用）和 `opencv-python`（读视频抽帧）。torch / numpy / Pillow / aiohttp 由 ComfyUI 提供；音频读写走 ffmpeg 管道，**不需要 torchcodec、imageio 主包、本地提示词模型或额外 AI 依赖**。

然后**完全重启 ComfyUI**（关闭进程再启动，不是刷新网页）。在节点搜索中输入：

```
H3 漫剧导演台·一体节点
```

## 基本接线

导演台左侧需要连接：

- `model`：唯一的大模型接口。把外部 `UNet加载器` 的输出连接到这里；需要切换 Ref2VA 或 FL2VA 使用场景时，只更换上游加载器选择的模型文件，插件会根据主模型类型自动选择对应 conditioning 和续接方式。
- `clip`：H3 文本编码器（Qwen3VL）。
- `vae`：H3 视频 VAE。
- `audio_vae`：H3 音频 VAE。
- `width` / `height`：输出分辨率（建议接 Resolution Selector）。
- `自动续接`：主 `model` 为 FL2VA 时使用 FL2VA 硬首帧；主 `model` 为 Ref2VA 时使用 Ref2VA 软参考，不需要第二个模型接口或手动选择。
- 原来的「续接方式」仍由插件自动决定。旧工作流中的隐藏「每段后卸载模型」只保留输入槽位兼容，加载后会强制关闭且后端忽略；节点始终只返回轻量预览，完整成片由磁盘 MP4 自动合并。

导演台右侧输出：

- `images`：只返回最后一个已完成段的尾帧，供 ComfyUI 预览与下游轻量检查。
- `audio`：返回静音占位；各段完整音频保存在 MP4，并进入自动合并成片。
- `fps`、`frame_count`：帧率与总帧数。
- `report`：运行报告文本。

### 从 2.13.x 工作流升级

- 旧工作流中的节点名和分段 JSON 保持兼容，打开后会自动生成并保存项目 ID。
- 旧工作流残留的第二模型输入和 `ref_image_N` 外部图片输入会在界面恢复时自动移除；把当前要使用的 Ref2VA 或 FL2VA 模型接到唯一的 `model` 输入即可。
- 参考图只在导演台资产库和“当前段实际参考”中管理，不再从节点左侧连接图片输入。
- 用户的 4.0 类加速工作流若使用 `TE-Speed-MiniMaxH3` 和 `ComfyUI-MiniMaxH3-TeaCache`，目标电脑也必须安装这两个外部自定义节点；它们不是本插件自带依赖。

## 分段与时间轴

1. 时间轴上每段是一个格子，标注 `段N · 起止秒数`；三个页面都可拖动格子右缘或点击时长标签，以 1 秒步进在 `2–15` 秒内调整。
2. 点选格子后，下方编辑区显示该段的提示词、参考图、音频与预览。
3. 直接在 ComfyUI 里运行节点：全部段按顺序生成，配置未变的段自动跳过；只勾一段的「启用」即只重跑该段。
4. 每段生成后立即写入 `output/video/h3director/<project_id>/漫剧_segN_00001_.mp4`，并在同一项目目录保存尾帧 `tail_segN_00001_.png` 供下一段接力——中途重启 ComfyUI 也不丢进度。

## 参考图与 @图N 语法

- 段 1 至少上传 1 张参考图；后续段可继续沿用（勾选续接尾帧即可），也可每段换图。
- 参考图编号规则：`Picture 1` 起按顺序编号——续接尾帧排最前，然后是本段上传图；节点内蓝条会实时显示当前编号对照。
- 提示词中写 `@图1` / `@图2`（兼容 `@image1`、`@img1`、`【图1】`），运行时自动转为 `<Picture 1>` / `<Picture 2>`。
- 更稳的方式：光标放在提示词里，直接点缩略图，自动插入正确编号。
- 换场景/换人物：取消"续接上段尾帧"，上传新图，改写提示词即可（编号前移，建议点缩略图重插）。

## 全局提示词（v2.13+）

节点顶部有一个**全局提示词**输入框（创作 / 视频 / 文本三个界面通用，隐藏在节点参数里）。它解决的问题是：很多提示词要素（全片美术风格、世界观设定、固定角色外观描述）每段都要写一遍。

- 填写后，运行节点时全局提示词会**自动拼到每一段提示词的最前面**（段提示词整体作为后半部分），再走 `@图N` 转换与 `<Picture N>` 注入——单段提示词里不用再重复这些常量。
- 留空则不拼接，完全退回"每段独立提示词"的行为。
- 典型用法：把 `Pixar-inspired 3D cartoon, soft lighting, 24fps` 这类全片统一风格写进全局框，每段只写当段动作/台词。
- **文本界面自动提取（v2.13.1+）**：全局框下方有「解析时自动提取共享提示词到全局框」勾选框（默认勾选）。勾选后，只有用户明确点击替换全部、导入到当前段或追加到末尾时，插件才会扫描导入内容、自动抽出安全的跨段共享身份、风格和限制并填入全局框；**单独点击「📂 载入文本」只更新脚本框，不解析、不修改全局框或现有段**。用户手填的全局内容不会被覆盖。

## 视频界面（参考视频驱动，v2.0+）

点编辑区的「⇄ 切到视频界面」：**上方放参考视频，下方放照片**——H3 原生支持参考视频（最多 3 路，2~15 秒）：

- **白模→成片**：上方放白模/灰模渲染视频（动作+运镜），下方放人物/场景照片，生成的视频沿用白模的动作和运镜、外观全部来自照片和提示词。
- **照片人物替换视频人物**：上方放原视频，下方放新人物照片，提示词写明替换关系。
- 点视频块自动插入 `<Video N>` 标签；后端自动追加官方 `reference` 结构化声明（动作/运镜/节奏跟视频，外观跟照片），不用手写。
- 参考视频默认选择「不发送音轨（推荐）」：只读取动作、构图、运镜和可见节奏，避免原视频杂音、伪语言或对白污染新视频。只有确实需要复刻原节奏/原台词，且原音轨干净时，才在当前段把「参考视频音轨」切换为「同时参考音轨」；白模无音轨也能正常使用。

> 提示：参考视频最长边预缩到 1280 控内存；段时长建议与参考视频时长相近。

## 文本界面（纯提示词生成 + 分镜脚本一键导入，v2.11+）

顶栏第三个页签「文本界面」：默认**不需要任何参考视频、参考图、配音、音色**——画面、人物、动作、运镜、台词、音效全部由 H3 按提示词原生生成。纯文本、无参考素材时可把 FL2VA 接到唯一的 `model` 输入执行 T2VA/FL2VA conditioning。与创作/视频界面数据完全独立，产出文件名也是独立的（`漫剧t_segN` / `tailt_segN`），三页互不覆盖。

核心用法是**分镜脚本解析导入**：把带时间标记的分镜文本粘进顶部脚本框，检查内容后选择「解析并替换全部段」「导入到当前段」或「追加到末尾」，插件再自动拆段、按标记设好每段时长、把对应正文写进每段提示词。「📂 载入文本」只选择本地 `.txt` 并填入脚本框，不会自动改变当前时间线。支持的标记写法（每段一个标记行）：

| 写法 | 示例 | 时长来源 |
|---|---|---|
| 段标记带时长 | `段1（6.6秒）：黄昏屋顶，少年转身……` | 括号里的秒数 |
| 段标记无时长 | `段1` + 正文里写 `[0s-3s] …` 时间轴 | 正文时间轴的最大结束秒 |
| 区间标记 | `[0s-6.6s] 开场……` / `0:00-0:06 …` / `0至6秒：…` | 区间差值 |
| 时长直写 | `6.6秒 \| 独白一场……` | 竖线前的秒数 |
| 官方 Shot 格式（v2.13.5+） | `integrated_multimodal_description: [Shot 1] … [Shot 2] At 00:00:03.000, …` 整段直贴 | `At HH:MM:SS.mmm` 绝对时间戳差值（末镜优先使用导入清单或明确范围终点） |

- 官方 Shot 格式（v2.13.8 起改为「按 15s 合并段」）：H3 单次能原生生成 ≤15s，且官方格式内部就用 `[Shot N] At mm:ss` 排子镜头时间轴——所以**总时长 ≤15s 的片子不再按 Shot 拆成多段，而是合并成「一段 = 一次生成」**，段提示词保留 `[Shot N] At` 结构让 H3 自己卡内部节奏；风格句 + `overall_soundscape` / `non_diegetic_music` 也一并并入这一段，成为完整自包含的官方提示词（不依赖全局框）。**总时长 >15s** 才按贪心把连续 Shot 装进 ≤15s 的生成桶（桶内 Shot 重新相对桶起点从 0s 计时），此时风格句与音效/配乐字段进全局提示词框、由各生成桶共享。
- 混用时以「段N」类标记为准切分（正文里的 `[0s-3s]` 时间轴标签不会误拆段）。
- 创作、视频、文本三个页面的生成段时长统一保存为 2–15 的整数秒；后端执行时自动换算为最接近的 H3 合法帧数。超过 15 秒的段会被截到 15 秒。
- **整篇没有任何标记的长文案（v2.13.9+）：按朗读时长智能分段**——直接粘贴整篇小说/旁白稿（不写任何标记），解析时自动按标准语速（4.5 字/秒）估算每句时长，切成**每段 8~15 秒**；断点优先级 = 段落换行 > 句末标点（。！？… .!?）> 从句标点（，、：），**绝不在句中硬断**（仅无标点超长句按字数兜底切），每段都是完整的镜头/情节描述，模型好理解。3 分钟文案（约 800 字）会自动分成十几段。
- 段间默认续接上一段尾帧（跨段动作连续），可在段行取消；段行还有独立的时长/种子。
- **种子随机范围选择**（v2.13.12+）：段行种子输入框左侧新增下拉，控制 🎲 按钮行为——**当段随机**（默认，只改当前段）、**全段各自随机**（所有段各换一个新种子，画面差异大）、**全段统一随机**（所有段共用同一个新种子，角色/风格更一致）。该选择随工作流保存，创作/视频/文本三界面通用。
- 脚本框内容随工作流保存；「📄 格式骨架」只提供可直接编辑的官方 Base 示例，「📂 载入文本」可选择本地 `.txt` 并载入脚本框。
- 「⚡ 解析导入」是整篇解析：用脚本解析结果替换当前页面的分段；已有内容时需再次点击红色确认按钮。文本页与创作页分别维护自己的脚本和分段，不互相覆盖。
- 手工编辑脚本后直接运行时，v2.24.24 会先自动执行同一套整篇解析；解析失败或时长明显膨胀时不会运行旧时间轴。
- 创作页的「资产库」与「当前段实际参考」分开：文件名默认作为资产名，可设置资产类型。资产不会按提示词自动绑定；把图片拖入当前段后才成为该段参考。每张资产可勾选「全局参考」，勾选后进入所有创作分段，不勾选则只保留在资产库或手动拖入过的分段。两个区域均可独立拖动调整宽高。
- 文本页、创作页、视频页是独立工作区。资产库只属于创作页，不会出现在文本页或视频页。
- 时间轴段块的时长数字可点选：点段块左下的「10s」弹出整数秒选择器，当前值高亮，点选即改；右缘拖拽调时长仍然可用并按整数秒吸附。

## 参考音色槽（多角色，A1~A3）

参考图下方有**参考音色槽 A1~A3**。当前界面中的这些槽按 `reference` 关系使用：只学习声音音色和表达方式，台词内容仍由提示词指定；旧工作流中保存过的槽级配音模式字段仍由后端兼容读取。

- 点槽块自动在提示词插入 `<Audio N>`；也可手写 `@音1` / `@音2`（自动转 `<Audio N>`）。
- 提示词里把音频和人物绑定即可，如：`<Audio 1> 是 <Picture 1> 角色（S1）的音色参考`。
- H3 最多 3 路独立参考音频：本段音频的「参考驱动」占第 1 路时，音色槽编号自动从 A2 起（面板有提示）。
- 要让角色 1:1 复用一段配音并对口型，请使用本段音频的「参考音频驱动」，而不是参考音色槽。

## 环境音（重要认知）

参考音频（配音/音色槽）的条件会压制 H3 自生成环境音——提示词写"风声鸟鸣"也常常不出（实测参考音频贯穿采样全程，模型只顾模仿人声音色）。

**想要 H3 原生环境音/音效：该段不要挂任何参考音频**，纯提示词驱动（模型音频模式），
环境音写具体拟声（`wind chime rings, ding, ding`）最有效。实用策略：对话段挂音色槽保台词，空镜/转场段不挂、专门出环境音，交替排布。

## API 设置

顶部 **API 设置** 只使用你主动配置的 **OpenAI-compatible API**。当前前台用途是连接测试，以及创作页在文件名、资产名和别名仍无法确定时的资产匹配；本地确定性名称匹配不需要 API，也不会下载或加载本地提示词模型。

- 在顶部 API 设置中可选择 CodexCN、OpenAI、DeepSeek、OpenRouter、硅基流动或阿里云百炼，Base URL 会自动填写；其它中转站选择「自定义 / 中转站」。
- 模型选择提供各服务商常用模型快捷项；列表没有的新模型选择「自定义模型」，再手动填写模型 ID，不会被固定列表限制。
- 填写 `API Key` 后点「**保存设置**」。
- 点「**测试连接**」会保存当前设置并发送一次最短文字测试；只有主动测试或创作页确实需要不确定资产匹配时才联网。
- 自动资产匹配发送的是分段文字以及资产稳定 ID、名称和类型，不发送本地文件路径，也不把整个资产库无条件塞入每一段。
- API 离线、未配置或漏回某段时，该段保持本地匹配结果或空缺，不会用完整资产库兜底。明确写“无人、空镜、人物离场”的段不会被 API 加回角色。
- API Key 只保存在本机 `ComfyUI/user/ComfyUI-H3-Director/api_config.json`；前端接口只查询是否已配置，Key 不写入工作流、节点属性、localStorage 或日志。
- Base URL 通常填写到 `/v1`，插件会自动调用 `/chat/completions`；如果服务商给的是完整 `/chat/completions` 地址，也可以直接填写。

## 段级音频

每段音频来源二选一（v1.7.3 起；旧工作流里的"替换/混合"段会显示"旧版"选项兼容）：

| 模式 | 说明 | 口型 | 需要上传 |
|---|---|---|---|
| 模型音频（H3 生成） | 默认，H3 自己生成的声音和环境音 | 同步 | 否 |
| **参考音频驱动（v1.6+，配音推荐）** | 上传配音喂给 H3 当生成条件（`<Audio 1>`），模型听着它说台词 | **原生同步** | 是 |

> **想让角色说你的配音且口型对上，选「参考音频驱动」**。替换/混合只是把音轨贴到成片上，
> 嘴部动作仍按模型自己的节奏动，必然错位——这是原理决定的，不是 bug。
>
> 参考音频驱动有两个子模式（v1.6.1+，按 MiniMax 官方 R2V 提示词指南实现）：
>
> | 子模式 | 官方关系 | 效果 |
> |---|---|---|
> | 复刻音轨（默认） | `fully_copy` | 你的配音 1:1 用作成片音轨，画面口型对齐它（配音台词首选） |
> | 复刻音轨 + 保留 H3 环境音（v1.7+ 勾选） | `partially_copy` | 配音作对话轨 1:1 复用，H3 同时生成环境音/音效/配乐 |
> | 仅音色参考 | `reference` | 模型只学音色语气，台词按提示词重新生成（声音克隆） |
>
> 插件会按子模式自动向提示词追加官方结构化声明（`subject_definitions` / `retention_analysis` /
> `detailed_description` + `<Audio 1>` 标签）——没有这段声明模型会忽略参考音频（v1.6.0 实测踩坑）。
> 裁剪/挖洞/起始偏移在此模式同样生效（先处理音频再喂给模型），音量不适用。
> 建议配音时长与段时长接近（音长警示会提示差距）。

- 上传后显示**波形编辑器**：拖双柄裁剪（保留选中区 / 删除选中区中间挖掉），支持 `wav/mp3/m4a/ogg/flac/aac`。
- `起始偏移`：配音从段内第 X 秒开始播（自动对齐段时长，长截短静音）。
- 采样率/声道自动统一（TTS 常见的 22050Hz mono 也能直接与 32000Hz stereo 模型声混合）。
- 时间轴下方的 `AUDIO` 轨道显示每段音频状态，点"开/关"可让某段输出无声视频。
- **三种添加方式**（v1.5+）：`+音频` 按钮上传；**把音频文件直接拖到 AUDIO 轨道的段块上**；或从**音频库下拉**直接选用 input 目录里已有的音频（不用重复上传）。
- **音长警示**（v1.5+）：音频有效时长盖不住段长（尾部无声）或超出段长（被截断）时，AUDIO 段块和裁剪区会显示黄色 ⚠ 提示。
- **台词时长建议**（v1.5+）：提示词里用引号写台词（`"台词"`），编辑区顶部实时显示三档语速（慢/标准/快）的整数秒建议时长，点击即应用——写对话戏不再靠猜时长。

## 预览与逐段验收

- 点选段落，编辑区底部出现该段的视频播放器（带声音、可拖大、可新窗口放大）。
- 一键运行全部段后会自动合并；逐段过一遍时若某段不满意，直接点该时间线段卡里的「重抽」，只替换这一段，结束后自动用其他段的原片重新合并。
- 「合并成片」可随时手动重建；成功后会在导演台内直接播放。「查看成片」始终显示，当前界面已有合并 MP4 时可随时重新打开。创作、视频、文本三套产出和合并文件彼此独立。
- 面板顶栏常显版本号；若状态栏出现红色版本警告，说明前端/后端有一边是旧代码，按提示重启或强刷即可。

## 时长、帧数与帧率

- H3 固定按 **24 帧/秒** 生成，帧数自动对齐到 `17n+5`，最终时长与填写值可能有 ≤0.5 秒差异。
- 每段可单独设置**输出帧率 8~24**：调低为抽帧（时长不变，呈逐帧动画感）。各段帧率不同时合流输出会自动统一。

## 输出分辨率

建议配合 Resolution Selector 使用：默认 `16:9 / 0.4MP / 32`（约 832×480，与 H3 原工作流一致，16GB 显存实测每 10 秒段约 8~12 分钟）。追求画质可升 `0.9MP`（约 1280×704），耗时与显存相应增加。

## 输出文件位置

每个导演台节点首次创建时会生成持久化 `project_id`。全部产物按项目隔离在：

```
ComfyUI/output/video/h3director/<project_id>/
```

创作界面示例：

```
漫剧_seg1_00001_.mp4    第 1 段成片（每段独立；创作界面）
漫剧_seg2_00001_.mp4    第 2 段成片
tail_seg1_00001_.png    第 1 段尾帧（接力用，无需管理）
漫剧_segN_00001_.json   第 N 段配置缓存（哈希跳过用，无需管理）
```

视频界面产出独立命名（`漫剧v_segN` / `tailv_segN`），文本界面同样独立（`漫剧t_segN` / `tailt_segN`），三个界面的成片与缓存互不覆盖。

### 自动合并与内存

- 界面已移除高内存“汇总输出”下拉框。各段 MP4 始终保留完整画面和音频；节点 `IMAGE` 只返回最后一段尾帧，`AUDIO` 返回极短静音占位，`frame_count` 仍统计所有已完成段折算到 24fps 的总帧数。
- 点击“运行”后会在所有启用段完成时自动合并；点击时间线段卡里的“重抽”后会覆盖该段固定文件，并自动按全部启用段的最新文件重新合并，因此能得到 `1-2-新3-4`。顶部“合并成片”按钮仍作为手动补救。
- 这种方式不会把整片所有帧与音频重新读回 Python 张量，长项目和普通内存环境更稳定。
- 单段 MP4 写出同样采用逐帧 FFmpeg stdin 流：Python 不再额外保存一份整段 RGB `bytes` 副本，从而降低的是**系统内存**编码峰值；模型采样和 VAE 解码所需的**显存**不会因此减少。

## 模型文件（插件不含，需单独下载）

与原 MiniMax H3 工作流相同，放入对应目录：

- `models/diffusion_models/minimax_h3_ref2va_pruned_int8_convrot.safetensors`
- `models/diffusion_models/minimax_h3_fl2va_pruned_int8_convrot.safetensors`（需要自动启用硬首帧续接或使用纯文本 FL2VA/T2VA 时安装）
- `models/text_encoders/qwen3vl_32b_minimax_h3_int8_convrot.safetensors`
- `models/vae/minimax_h3_video_vae_fp16.safetensors`
- `models/vae/minimax_h3_audio_vae_fp32.safetensors`

## 素材上传限制

- 参考图：常见 `jpg/png/webp`，由 ComfyUI 内置图片上传接口接收，建议单张不超过 10MB。
- 自定义音频：`wav/mp3/m4a/ogg/flac/aac`，后端限制 **100MB/文件**；内容时长建议与单段接近。
- 参考视频/续接视频：`mp4/webm/mov/mkv/avi`（续接主要使用 mp4），后端限制 **2GB/文件**；实际可上传上限还会受浏览器、反向代理和 ComfyUI 服务配置影响。
- 上传素材保存在 ComfyUI 的 `input` 目录，工作流只保存文件名，不写入媒体二进制。

## 外部低显存与加速节点筛选

本插件没有把用户提供的 4 步/6 步大工作流整套复制进来，也没有新增强制 custom node。原因是导演台的目标是把稳定的素材分配、分段缓存、重抽和合并能力集中在一个节点中；把 Krea2、Z-Image、Yuan、MultiImageLoader、GGUF、ControlNet 和多套 LoRA 一起带入，会显著增加缺节点、缺模型、爆显存和版本冲突的概率。

v2.36.1 的验证顺序固定为：先复测同一 5 秒成功路径，再测试 15 秒；只有资源交接修复后的 15 秒仍然 OOM，才单独进入 KJNodes 低显存节点真实 A/B。KJNodes 没有被自动集成、复制或设为插件依赖，本轮也没有执行该 GPU A/B。

需要降低显存或加速时，可在上游工作流中自由连接第三方节点。导演台不会检查、阻止、警告或改写这些节点：

1. 优先使用官方/Comfy-Org 的 INT8 H3 模型，不要把完整高精度模型常驻显存。
2. 如已安装较新的 `ComfyUI-KJNodes`，可选 `MiniMaxChunkFeedForward`（例如 chunks=4、seq_threshold=4096）。它按 token 分块降低前馈峰值显存，但仍标记 experimental；导演台只诊断，不自动追加。
3. 仍缺显存时再单独尝试 `MiniMaxLowVRAMAttention`。它按 attention head 分组并尽早释放中间张量，源码声明不改变数学结果，但同样是 experimental。
4. `SolAttnPatch`、Memory-Efficient SageAttention 等注意力替换只作为可选 A/B；不要一次叠加多套。社区曾报告部分 GPU/版本使用 H3 Memory-Efficient SageAttention 时无报错却输出灰噪，因此导演台不会自动开启。
5. Turbo LoRA、剪枝模型和外部 TurboSampler 也可自由接入；具体步数、音频/video flow schedule、兼容性和输出质量由所连接的第三方节点与模型共同决定，导演台不做额外判断。

导演台只消费最终接入的 `MODEL`，不会替用户自动改线、安装节点或干预上游模型链。

## 常见问题

**改了代码/界面没变化**
前端改动：浏览器 `Ctrl+Shift+R` 强刷；后端改动：完全重启 ComfyUI 进程。面板顶栏有版本号，可对照确认。

**预览黑屏 0:00**
老版本生成的视频缺少 faststart 头，重跑该段即可（v1.3+ 生成的视频均自带）。

**换场景后画面发糊**
参考图驱动模型的正常表现：没有尾帧照画时，模型需要凭空构造新场景。建议新场景段落升到 0.9MP，并把场景图放在参考图第一位。

**提示词里出现 `<Picture N>` 英文标签**
这是 H3 识别参考图所需的官方控制标签，正文仍可全部用中文书写。

**导演台有声音破碎/杂音，官方原生工作流正常**
2.20 已统一修复音频写出和合并参数。若单段刚生成时仍有破碎声，可自行旁路上游第三方加速节点并与原生模型链对照；导演台不会扫描或干预已经进入 `MODEL` 对象的外部修改。

**运行时报 `Command '[...triton...tcc.exe', ...]' 失败（Windows + Python 3.13）**
报错来自 H3 模型推理链路里的 Triton 即时编译（triton-windows 用自带的 tcc 链接 CUDA / Python 库），与本插件代码无关，属于整合包环境问题，按顺序尝试：
1. 把整合包 Python 的 `libs\python313.lib`（通常在 `python_embeded\libs`，若无则从同版本官方 Python 拷贝）放到可被 tcc 找到的位置，或复制到 `python_embeded\libs` 目录下；
2. 确认已安装 CUDA Toolkit（报错命令行里引用了 `CUDA\v12.x`），并把 `CUDA\bin` 加入 PATH；
3. 在 H3 采样相关节点里把注意力实现从 Triton/SageAttention 切换为 SDPA（如节点提供该选项），绕开 Triton 编译；
4. 仍不行：把完整报错文本（不是截图）发给 H3 整合包/模型节点的作者——编译命令的最后几行才是真正的失败原因。

**面板变窄/比例不对（v1.4.1 已修复）**
低画布缩放下拖拽位移未换算导致的跳变，及个别前端版本下面板宽度脱离节点宽度的问题，已在 v1.4.1 修复：所有拖拽按画布缩放换算，并加了宽度看门狗自动校正。遇到旧版残留状态，把节点拉大一次即可恢复。

## 致谢

本插件基于 ComfyUI 原生 MiniMax H3 节点接口进行组合开发。感谢 ComfyUI 与 MiniMax H3 相关开发者提供底层模型支持。时间轴交互形态参考了 [ComfyUI_Bernini_Director](https://github.com/AIMixer/ComfyUI_Bernini_Director)。

本次资产库、官方字段和冲突诊断主要核对了以下公开资料：

- [MiniMax-AI/MiniMax-H3 官方仓库与 skills](https://github.com/MiniMax-AI/MiniMax-H3/tree/main/skills)
- [MiniMax H3 官方提示词技能](https://github.com/MiniMax-AI/MiniMax-H3/tree/main/skills/h3-prompt-writing)
- [ComfyUI-KJNodes](https://github.com/kijai/ComfyUI-KJNodes)
- [H3 Memory-Efficient SageAttention 灰噪问题记录](https://github.com/kijai/ComfyUI-KJNodes/issues/712)
- [MiniMax H3 TurboSampler/LoRA 兼容说明](https://github.com/cicalooo/ComfyUI-MiniMax-H3-TurboSampler)
